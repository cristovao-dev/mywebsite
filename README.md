# Cristovao Freitas Portfolio

A lightweight GitHub Pages portfolio presenting skills, profile links, and public GitHub projects.

## Local preview

Open `index.html` in a browser. No build step is required.

## GitHub Pages

In the repository settings, enable GitHub Pages from the `main` branch and root folder.

The apps section loads public repositories from `cristovao-dev` through the GitHub public API.
